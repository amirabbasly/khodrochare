# توسعه عمق محتوا — همه پست‌ها ۱۰۰۰+ کلمه + لینک‌سازی گسترده

تاریخ: ۱ مهر ۱۴۰۵ (۲۰۲۶-۰۹-۲۳) — دنباله `docs/seo-final-stage-2026-09-23.md`

## خلاصه

- هر **۵۴ پست وبلاگ** به حداقل ۱۰۰۰ کلمه محتوای مرئی رسید (کمینه واقعی: ۱۰۷۷).
- **لینک‌سازی داخلی گسترده:** قوانین متنی خودکار ۴۱ ← ۹۵ (مدل‌ها، برندها، محله‌ها،
  خدمات، راهنماها)، سقف لینک متنی هر مقاله ۱۲ ← ۱۸، پوشش ۱۰۰٪ باکس لینک تجاری
  (۴۷ رکورد)؛ هر مقاله اکنون ~۳۰ لینک داخلی دارد.
- صفحه **امداد خودرو آنلاین** (۱۹۴۷ کلمه)، ستون **امداد خودرو** (۱۷۱۸) و ستون
  **شمال** (۱۴۴۷) توسعه یافتند.
- هر **۱۲ صفحه خدمت** با ۲ پاراگراف + ۲ چک + ۲ سؤال متداول تازه غنی شد
  (نمونه واقعی: ۱۲۳۵ تا ۱۳۹۵ کلمه).
- `updatedAtIso` همه پست‌ها به ۲۰۲۶-۰۹-۲۳ رفت → سیگنال تازگی در `lastmod` نقشه سایت.
- شمارش‌ها ثابت: ۵۴ پست، ۴۱۹ مسیر، بدون صفحه جدید — این دور فقط «عمق» بود.

## معماری اجرا

- محتوای تازه در ۴ فایل `src/content/blog-expansion-{a,b,c,d}.ts` (کلیدخورده با slug)
  و از طریق `supplementalArticleSections` در `src/content/blog.ts` به انتهای مقالات
  الصاق می‌شود؛ تعریف ۵۴ پست دست‌نخورده ماند.
- `wordCount` اسکیمای مقاله حالا سؤالات متداول را هم می‌شمارد (دقیق‌تر).
- تست‌های تازه: کف ۱۰۰۰ کلمه برای هر پست، پوشش قوانین لینک، غنای دانش خدمات.

## اعتبارسنجی (همه سبز)

- `test:unit` → ۴۵/۴۵؛ `typecheck` و `lint` سبز؛ `build` موفق.
- `test:seo` → ۴۱۹ صفحه، ۰ خطا (همه لینک‌های تازه resolve می‌شوند).
- راستی‌آزمایی HTML واقعی production: آنلاین ۱۹۴۷، ستون ۱۷۱۸، شمال ۱۴۴۷،
  نمونه خدمات ۱۲۳۵+، لاغرترین بلاگ ۱۴۹۱ کلمه با ۳۲ لینک داخلی.

## قدم بعدی پیشنهادی (دور بعد)

- لندینگ‌های شهر/جاده/برند (هاب‌های لینک): غنای هدفمند با داده محلی یکتا.
- تداوم: هر پست تازه از ابتدا با استاندارد ۱۰۰۰+ کلمه و ۳+ لینک منتشر شود.
