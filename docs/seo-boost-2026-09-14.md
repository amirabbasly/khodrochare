# تقویت سئو، لینک‌سازی داخلی و محتوا — ۲۳ شهریور ۱۴۰۵

درخواست مالک: پیدا کردن برنچِ دارای آخرین نسخه، تقویت لینک‌سازی داخلی،
مقاله‌ها و قدرت سئو بدون به‌هم‌ریختن UI صفحه اصلی، نمایش زودتر مودال تماس،
و پوش سورس روی گیت همراه گزارش.

## نسخه مبنا

- تنها برنچ‌های موجود `main` و `arena/01a09fa2-khodrochare` بودند و هر دو روی
  یک کامیت (`40977d6`) قرار داشتند؛ یعنی آخرین نسخه همین‌جا بود و کار روی
  همین برنچ جلسه انجام شد. برنچ جدیدی ساخته نشد.

## چه چیزی اضافه شد

### ۱. شش راهنمای تازه (`src/content/blog-seo-boost.ts`)

| مقاله | هدف سئو |
|---|---|
| `tehran-karaj-freeway-breakdown-guide` | دم‌دراز آزادراهی؛ حلقه به تهران/کرج/یدک‌کش |
| `jumpstart-vs-battery-replacement` | نیت تجاری باتری در برابر صفحات فروش رقبا |
| `ev-hybrid-roadside-safety-guide` | تمایز؛ موضوعی که رقبا ندارند |
| `car-lockout-key-guide` | فعال‌سازی خدمت کم‌لینک `vehicle-access` |
| `pre-purchase-car-inspection-guide` | فعال‌سازی `mobile-diagnostics` و `pre-trip-check` |
| `tehran-night-roadside-guide` | «امداد شبانه تهران» و امنیت توقف |

- هر مقاله ۷–۸ بخش متمایز، جدول/بولت، منابع https، ۳ سؤال متداول قابل مشاهده،
  `links` به مقصدهای واقعی و `relatedSlugs` معتبر دارد.
- تاریخ انتشار/ویرایش واقعی `2026-09-14` (۲۳ شهریور ۱۴۰۵)؛ بدون قیمت، زمان
  اعزام یا شعبه ساختگی.
- مقاله‌ها در **انتهای** مجموعه اضافه شدند؛ ترتیب ۵ تای اول مجله صفحه اصلی
  عوض نشده و UI صفحه اصلی دست‌نخورده است.

### ۲. لینک‌سازی داخلی

- `src/app/blog/[slug]/page.tsx`: لینک تجاری ۶ مقاله جدید + ۹ قانون لینک متنی
  تازه (امداد خودرو شمال/رشت، جاده چالوس، محور هراز، انتخاب یدک‌کش یا خودروبر،
  کد خطای دیاگ، نشانه خرابی، بازیابی دسترسی خودرو، بازدید پیش از سفر).
- `src/app/services/[slug]/page.tsx`: مقالات مرتبط همه ۱۲ خدمت، از جمله
  `vehicle-access` و `pre-trip-check` و `fuel-delivery` که قبلاً پوشش نداشتند.
- `src/components/seo/location-landings.tsx`: بخش «راهنمای تصمیم‌گیری»
  صفحات شهر به `/tools` و `/pricing` هم لینک می‌دهد.
- `src/components/site/site-footer.tsx`: لینک سراسری `/tools` و `/tools/obd-code`.
- `src/seo/keywords.ts`: افزودن «امداد خودرو شمال» و «امداد خودرو رشت».

### ۳. مودال تماس (`welcome-modal.tsx`)

- `showAfterMs` از ۴۵۰۰ به **۱۵۰۰ میلی‌ثانیه** رسید؛ همه نگهبان‌های نمایشی
  (در حال تایپ‌نبودن، نبود دیالوگ دیگر، مرئی‌بودن تب، محدودیت هر سشن) سر جایشان‌اند.
- کارت همچنان کوچک، گوشه‌صفحه، بدون اورلی/قفل اسکرول/دزدیدن فوکس و قابل بستن
  با Escape است؛ مصداق اینترستیشال مزاحم گوگل نیست.
- تست مرورگری زمان‌بندی (`home-experience.spec.ts`) با تأخیر جدید هماهنگ شد.

## اعتبارسنجی

- `npm run lint -- --max-warnings=0` ✅
- `npm run typecheck` ✅
- `npm run test:unit` — ۴۰/۴۰ ✅ (تست رشد محتوا برای ۳۹۰ مسیر و ۳۰ مقاله به‌روز شد)
- `npm run build` ✅
- `npm run test:seo` — ۳۹۰ صفحه ایندکس‌پذیر، صفر خطا، همه صفحات قابل دسترس ✅
- `npm run indexing:export` — فهرست ۳۹۰ URL برای ایندکس ✅
- `npm run test:browser` در این محیط اجرا نشد (نیازمند نصب Chromium سیستمی)؛
  در CI روی همین برنچ اجرا می‌شود.

## قدم بعدی پیشنهادی (بعد از دیپلوی)

۱. دیپلوی همین برنچ روی سرور و راستی‌آزمایی ۶ URL جدید با کد ۲۰۰ روی دامنه.
۲. ثبت سایت‌مپ تازه در Search Console و درخواست ایندکس ۶ مقاله.
۳. پایش کوئری‌های «آزادراه تهران کرج»، «باتری به باتری یا تعویض» و «امداد شبانه تهران».
