# اصلاح سه بخش رابط کاربری — ۱۵ شهریور ۱۴۰۵ / 2026-09-06

## ۱. یدک‌کش هیرو

- دو چرخ با پره‌های مشخص و نشان رنگی، با دورهٔ ۱٫۴ ثانیه می‌چرخند.
- هفت شعار ارسالی مالک سایت در `src/content/home-highlights.ts` نگهداری می‌شوند: حمل تا تعمیرگاه رایگان، تضمین کمترین قیمت، تضمین بالاترین کیفیت، کارشناس مجرب، پوشش سراسری، پاسخ‌گویی ۲۴ ساعته در ۷ روز هفته و سیستم پیگیری شکایات ایمن.
- هر چهار ثانیه یک حباب پیام ظاهر می‌شود؛ دورهٔ کامل ۲۸ ثانیه است. حباب‌ها روی هم نمی‌افتند و اندازهٔ ظرف ثابت می‌ماند.
- دکمهٔ توقف موجود در هیرو، چرخ‌ها، حباب‌ها و سایر اجزای متحرک یدک‌کش را هم متوقف می‌کند.
- در حالت `prefers-reduced-motion` چرخ‌ها ثابت‌اند و اولین شعار خوانا می‌ماند؛ حباب‌ها همگی ناپدید یا هم‌زمان روی هم نمایش داده نمی‌شوند.
- این شعارها متن معرفی کسب‌وکار هستند؛ این تغییر هیچ موقعیت، اعزام زنده، شعبه یا نرخ جدیدی تولید نمی‌کند. محتوای محدوده‌های خدمت و ابزار محاسبهٔ هزینه تغییر نکرده‌اند.

## ۲. کارت صفحهٔ اصلی و تراز دسته‌بندی‌ها

- فقط کارت صفحهٔ اصلی با `RequestPreviewCard` جایگزین شده است: اطلاعات ثابت، بدون فرم ورودی، دریافت شماره مشتری، فراخوانی API، ثبت سفارش یا پیام موفقیت ساختگی.
- عنوان «آماده‌سازی درخواست امداد خودرو» حفظ شده است. در این کارت هیچ توضیحی دربارهٔ نمایشی‌بودن درج نمی‌شود؛ اقدام واقعی و مشخص آن «تماس برای امداد» با شمارهٔ `09123022064` است.
- تراز شبکه از پایین به بالا تغییر کرده است. در دسکتاپ، دسته‌بندی خدمات هم‌تراز بالای کارت درخواست و کنار لبهٔ پایین هیرو قرار دارد؛ در موبایل کارت‌ها زیر هم هستند.
- مقصد چهار دسته‌بندی، صفحات برند/شهر/خدمت، H1 اصلی و فرم مستقل `/امداد-خودرو-آنلاین` تغییر نکرده‌اند. فرم مستقل همچنان فقط با تأیید واقعی پذیرش، دریافت موفق را اعلام می‌کند.

## ۳. پنجرهٔ تماس سریع

- پنجرهٔ کوچک و غیرمسدودکننده روی همهٔ صفحات، حدود ۴٫۵ ثانیه بعد از ورود ظاهر می‌شود. اسکرول صفحه قفل نمی‌شود و تمرکز صفحه‌کلید از کاربر گرفته نمی‌شود.
- در هر نشست تب فقط یک بار نمایش داده می‌شود؛ پرچم `khodrochare:welcome-seen:v2` در `sessionStorage` است. حالت مسدودبودن storage نیز مدیریت می‌شود.
- در تب پنهان، هنگام تایپ در فرم، یا در حضور یک دیالوگ مسدودکنندهٔ دیگر، نمایش تا مناسب‌شدن وضعیت به تعویق می‌افتد.
- دکمهٔ اصلی شمارهٔ `09123022064` را با `tel:` باز می‌کند.
- دکمهٔ «ادامه برای نصب اپلیکیشن و مراجعه به سایت» به راهنمای موجود `/app` می‌رود؛ نصب خودکار یا نصب انجام‌شده را وانمود نمی‌کند.
- بستن با × و Escape ممکن است. پنجره در صفحهٔ کوچک و حالت افقی داخل viewport می‌ماند و در صورت نیاز خودش اسکرول دارد.

## کنترل کیفیت محلی

| بررسی | نتیجه |
| --- | --- |
| lint سخت‌گیرانه | موفق، بدون هشدار |
| تست واحد | ۱۹ موفق |
| build production و TypeScript | موفق؛ ۳۸۵ خروجی framework/resource/page |
| HTTP/SEO | ۳۷۴ صفحهٔ هدف + فروشگاه noindex، همگی ۲۰۰ |
| لینک‌ها و دارایی‌ها | ۳۷۵ مقصد، ۸۶ بررسی دارایی و ۳۷ گونهٔ تصویر بهینه‌شده |
| مسیر اشتباه و ریدایرکت | ۱۳ مسیر نامعتبر با ۴۰۴ واقعی؛ ۱۵ ریدایرکت سالم |
| تست مرورگر | ۷۰ موفق، بدون شکست یا skip: ۴۶ بررسی قبلی + ۲۴ بررسی جدید |
| خروجی ایندکس | همان ۳۷۴ URL، بدون تغییر |

گزارش HTTP: `2026-09-06T10:51:03.936Z`. تست‌های مرورگر محلی با Chromium 149 اجرا شده‌اند. کنترل عرض ۳۲۰ تا ۱۶۰۰ پیکسل، حالت افقی پنجره، چرخهٔ واقعی انیمیشن CSS، توقف، reduced motion، تأخیر پنجره، حفظ تمرکز، بسته‌شدن، مسیر نصب، نشست و storage مسدودشده در آزمون‌های جدید پوشش داده شده‌اند. مرورگر استاندارد CI جداگانه توسط workflow نصب می‌شود.

شواهد تولیدشده در `coverage/seo/production-report.json` و `coverage/browser/` قرار دارند و وارد Git نمی‌شوند. پیش‌نمایش sandbox با یک سرور کمکی موقت قابل نمایش است؛ تنظیمات امنیتی قاب‌بندی نسخهٔ production تغییر نکرده‌اند.

## استقرار

این تغییرات روی شاخهٔ `arena/01a072dc-khodrochare` منتشر می‌شوند. انتشار GitHub به معنی استقرار روی سرور اصلی نیست.

اگر سرور روی همین شاخه است، داخل `/var/www/khodrochare`:

```bash
git pull --ff-only origin arena/01a072dc-khodrochare &&
npm ci --include=dev &&
npm run build
```

سپس فقط بعد از موفقیت build، سرویس فعلی سایت را با همان مدیر فرایند سرور restart کنید. Node 22 توصیه می‌شود. `--include=dev` برای نصب وابستگی‌های لازمِ بررسی TypeScript و build مهم است. فایل `.env` و فایل تأیید مالکیت گوگل را حفظ کنید. برای بازدید مجدد پنجره، یک نشست/تب تازه باز کنید؛ بسته‌بودن آن در همان نشست رفتار مورد انتظار است.
