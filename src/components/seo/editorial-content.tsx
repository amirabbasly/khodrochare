import type { ContentSource, EditorialSection } from "@/content/editorial-types";
export function EditorialDetails({ section }: { section: EditorialSection }) {
  return <>{section.bullets?.length ? <ul className="mt-4 list-inside list-disc space-y-3 text-sm leading-8 text-slate-600">{section.bullets.map((item) => <li key={item}>{item}</li>)}</ul> : null}{section.table && <div className="mt-5 overflow-x-auto rounded-xl border border-slate-200"><table className="w-full min-w-[480px] text-right text-sm leading-7"><caption className="bg-slate-50 p-3 text-right font-bold text-ink">{section.table.caption}</caption><thead className="bg-slate-100 text-ink"><tr>{section.table.headers.map((header) => <th key={header} scope="col" className="p-3">{header}</th>)}</tr></thead><tbody>{section.table.rows.map((row, index) => <tr key={index} className="border-t border-slate-100">{row.map((cell, column) => column === 0 ? <th key={column} scope="row" className="p-3 font-bold">{cell}</th> : <td key={column} className="p-3 text-slate-600">{cell}</td>)}</tr>)}</tbody></table></div>}</>;
}
export function SourceList({ sources }: { sources: readonly ContentSource[] }) {
  return <section className="mt-8 border-t border-slate-200 pt-6" aria-label="منابع و حدود راهنما"><h2 className="text-xl font-black text-ink">منابع و حدود راهنما</h2><p className="mt-3 text-sm leading-8 text-slate-600">تهیه و تنظیم: تحریریه خودرو چاره. منابع زیر برای اصول عمومی و تعریف‌های فنی‌اند، نه تأیید خدمات کسب‌وکار یا جایگزین دفترچه خودرو و بازدید متخصص.</p><ul className="mt-4 space-y-3">{sources.map((source, index) => <li key={source.url} className="text-sm leading-8"><a href={source.url} target="_blank" rel="noopener noreferrer" className="font-bold text-brand-orange underline">[{index + 1}] {source.title}</a>{source.note && <p className="mt-1 text-xs leading-7 text-slate-500">{source.note}</p>}</li>)}</ul></section>;
}
